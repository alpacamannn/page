import { PrettyscalePipe } from './prettyscale.pipe';

describe('PrettyscalePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyscalePipe();
    expect(pipe).toBeTruthy();
  });
});
